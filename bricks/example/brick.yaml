name: example
description: An actual complete, working game example. Extra code that you won't need but will teach you how to wire the most important pieces.
version: 0.1.0
vars:
  name:
    type: string
  description:
    type: string
  version:
    type: string
  flame-version:
    type: string
  flame-lint-version:
    type: string
  extra-dependencies:
    type: array
  extra-dev-dependencies:
    type: array
