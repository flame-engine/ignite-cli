name: basics
description: The basic structure that most games will require. No boilerplate required, but no extra fluff.
version: 0.1.0
vars:
  name:
    type: string
  description:
    type: string
  version:
    type: string
  flame-version:
    type: string
  flame-lint-version:
    type: string
  extra-dependencies:
    type: array
  extra-dev-dependencies:
    type: array
