name: simple
description: An empty Flame project with just the bare minimums to get you up and running.
version: 0.1.0
vars:
  name:
    type: string
  description:
    type: string
  version:
    type: string
  flame-version:
    type: string
  flame-lint-version:
    type: string
  extra-dependencies:
    type: array
  extra-dev-dependencies:
    type: array
